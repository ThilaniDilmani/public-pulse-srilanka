# API Base URL (defaults to /api/v1 via Vite dev proxy)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# API Key for authenticated endpoints (leave empty for local dev if API_KEY is unconfigured)
VITE_API_KEY=
